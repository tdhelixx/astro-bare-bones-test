{
  "name": "astro-bare-bones-test",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev", 
    "build": "echo 'Starting build process...' && astro build && echo 'Build completed successfully!'",
    "build:debug": "echo 'Debug build starting...' && astro build --verbose && echo 'Debug build completed!'",
    "preview": "astro preview",
    "test": "echo 'Test passed - minimal setup working'"
  },
  "dependencies": {
    "astro": "^4.11.0"
  }
} 